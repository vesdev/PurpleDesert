window = glc_CreateWindow({width: 960, aspectRatio: 16/9, scalingMode: GLC_VERTICAL});
glc_SetWindow(window);
camera = glc_CreateCamera({width: 960, aspectRatio: 16/9, strict: false, scalingMode: GLC_VERTICAL});

glc_SetCamera(0, camera);



glc_SetCamera(0, camera);

show_debug_overlay(true);